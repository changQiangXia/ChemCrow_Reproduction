# CHANGE 003

## 1. 基本信息

- 编号：`CHANGE_003`
- 日期：`2026-03-14`
- 改动类型：运行时配置更新

## 2. 触发原因

- 用户提供了新的 `RXN4CHEM_API_KEY` 与新的 `RXN4CHEM_PROJECT_ID`。
- 用户明确说明当前新 key 的调用上限为 `100` 次。
- 后续验证策略需要相应调整为“最小必要调用优先”。

## 3. 更新内容

- 已更新仓库根目录 `.env` 中的：
  - `RXN4CHEM_API_KEY`
  - `RXN4CHEM_PROJECT_ID`

## 4. 记录约束

- 文档中不记录明文 key。
- 后续 RXN 验证应优先使用低消耗脚本。
- 在未形成明确必要性之前，不重复执行高消耗的逆合成完整链路。

## 5. 后续策略

- 当前优先验证新 `project_id` 是否可被项目列表识别。
- 当前不立即重跑 `ReactionRetrosynthesis` 完整链路。
- 后续若进入逆合成复测，应先明确该次调用的声明、验收标准与必要性。
